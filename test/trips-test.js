import { assert } from 'chai';
import Trip from '../src/trips'

describe('trip class', () => {

	// beforeEach(() => {
	// 	trip = new Trip()
	// })

	it('should be a function', () => {
		assert.isFunction(Trip)
	})

})